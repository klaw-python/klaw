[lint]
extend-ignore = [
    "N818",  # Exception name should end with Error - Propagate is intentional
]

[lint.per-file-ignores]
"tests/**/*.py" = [
    "S101",    # Allow assert in tests
    "PLC0415", # Allow imports inside functions (for import tests)
]
"conftest.py" = [
    "PLC0415", # Allow imports inside fixtures
]
