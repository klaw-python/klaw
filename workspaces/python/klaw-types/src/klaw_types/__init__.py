"""Klaw Types - Type definitions and protocols for the Klaw ecosystem.

This package provides type definitions, protocols, and type-related utilities
used throughout the Klaw ecosystem. It includes common type patterns, protocol
definitions, and type-safe utilities.
"""


def hello() -> str:
    """Return a greeting from klaw-types.

    This function demonstrates the basic functionality of the klaw-types package
    and serves as an example of type-safe operations.

    Returns:
        A greeting string from klaw-types.

    Example:
        ```python
        from klaw_types import hello

        result = hello()
        print(result)  # Hello from klaw-types!
        ```
    """
    return 'Hello from klaw-types!'
